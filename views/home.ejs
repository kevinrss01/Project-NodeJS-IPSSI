
<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" type="text/css" href="/css/style.css">
    </head>
    <body>
        <%- include('./header') %>
        <main>
            <div class="booksContainer">
                <% books.forEach(function(book) { %>
                    <% if (books.length === 0) { %>
                        <p>Il n'y a pas de livre dans la base de donnée</p>
                    <%} else { %>
                        <div class="bookCard">
                            <h3 style="text-align: center"><b><%= book.name %></b></h3>
                            <p>ISBN : <%= book.isbn %></p>
                            <p>Auteur : <%= book.author %></p>
                            <p>Description : <%= book.description %></p>
                            <p>Genre : <%= book.genre %></p>
                            <p>Price : <%= book.price %></p>
                            <p>Date de publication : <%= book.published %></p>
                            <a href='/book/<%= book._id %>'>Voir le livre</a>
                        </div>
                    <% } %>
                <% }); %>
            </div>
        </main>
    </body>

</html>
